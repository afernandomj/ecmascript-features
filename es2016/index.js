// Exponentiation Operator
let base = 2;
let exponent = 4;
let result = base ** exponent;

console.log(`${base} raised to the power of ${exponent} is ${result}`);
// Output: 2 raised to the power of 4 is 16
